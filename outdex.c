#include <stdio.h>
#include <math.h>

int main() {
    int distance = 8;
    int sisi = 6;
    double meow = sqrt(pow(distance, 2) + pow(sisi, 2));
    printf("The diagonal distance is: %f\n", meow);

    return 0;
}